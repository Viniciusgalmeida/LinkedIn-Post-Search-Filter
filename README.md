# LinkedIn-Post-Search-Filter
A lightweight static HTML tool that generates filtered LinkedIn search URLs for finding recent posts on a specific technical topic (e.g. TypeScript, Rust, Kubernetes). It lets you set a keyword, exclude terms (like job/hiring posts), and a time window (24h/week/month), then copy the ready-to-use URL to paste into your browser
